# =============================================================================
# ms-solicitudes — Configuración base
# CQRS Write Side | Arquitectura Hexagonal
# =============================================================================

# -----------------------------------------------------------------------------
# Servidor HTTP
# -----------------------------------------------------------------------------
quarkus.http.port=8080

# CORS deshabilitado: se gestiona exclusivamente en el api-gateway
# quarkus.http.cors.enabled=false  (valor por defecto)

# -----------------------------------------------------------------------------
# Datasource — PostgreSQL
# -----------------------------------------------------------------------------
quarkus.datasource.db-kind=postgresql
quarkus.datasource.username=${DB_USER:solicitudes_user}
quarkus.datasource.password=${DB_PASS:secret}
quarkus.datasource.jdbc.url=jdbc:postgresql://${DB_HOST:localhost}:5432/${DB_NAME:solicitudes_db}

# Estrategia DDL: 'update' en desarrollo, 'validate' o 'none' en producción
quarkus.hibernate-orm.database.generation=update
quarkus.hibernate-orm.log.sql=true

# -----------------------------------------------------------------------------
# Kafka — Bootstrap
# -----------------------------------------------------------------------------
kafka.bootstrap.servers=${KAFKA_BOOTSTRAP:localhost:9092}

# -----------------------------------------------------------------------------
# Kafka — Productor: solicitud.creada
# -----------------------------------------------------------------------------
mp.messaging.outgoing.solicitud-creada.connector=smallrye-kafka
mp.messaging.outgoing.solicitud-creada.topic=solicitud.creada
mp.messaging.outgoing.solicitud-creada.value.serializer=io.quarkus.kafka.client.serialization.ObjectMapperSerializer

# -----------------------------------------------------------------------------
# Kafka — Consumidor: solicitud.aprobada  (producido por ms-aprobaciones)
# -----------------------------------------------------------------------------
mp.messaging.incoming.solicitud-aprobada.connector=smallrye-kafka
mp.messaging.incoming.solicitud-aprobada.topic=solicitud.aprobada
mp.messaging.incoming.solicitud-aprobada.group.id=ms-solicitudes-aprobada-group
mp.messaging.incoming.solicitud-aprobada.value.deserializer=org.apache.kafka.common.serialization.StringDeserializer
mp.messaging.incoming.solicitud-aprobada.auto.offset.reset=earliest

# -----------------------------------------------------------------------------
# Kafka — Consumidor: solicitud.rechazada  (producido por ms-aprobaciones)
# -----------------------------------------------------------------------------
mp.messaging.incoming.solicitud-rechazada.connector=smallrye-kafka
mp.messaging.incoming.solicitud-rechazada.topic=solicitud.rechazada
mp.messaging.incoming.solicitud-rechazada.group.id=ms-solicitudes-rechazada-group
mp.messaging.incoming.solicitud-rechazada.value.deserializer=org.apache.kafka.common.serialization.StringDeserializer
mp.messaging.incoming.solicitud-rechazada.auto.offset.reset=earliest

# -----------------------------------------------------------------------------
# OpenAPI / Swagger UI
# -----------------------------------------------------------------------------
quarkus.swagger-ui.always-include=true
quarkus.smallrye-openapi.info-title=ms-solicitudes API
quarkus.smallrye-openapi.info-version=1.0.0

